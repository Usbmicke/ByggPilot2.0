# Detta är en exempelfil. För att applikationen ska fungera, kopiera denna fil till .env.local
# och ersätt platshållarna med dina egna, riktiga värden.

# Google Cloud & Firebase
# Dessa krävs för autentisering med Google och anslutning till Firestore.
# Du hittar dem i din Google Cloud Console och Firebase projektinställningar.
GOOGLE_CLIENT_ID="DIN_GOOGLE_CLIENT_ID_HÄR"
GOOGLE_CLIENT_SECRET="DIN_GOOGLE_CLIENT_SECRET_HÄR"

# NextAuth
# Detta är en hemlig nyckel som används för att signera JWT-tokens. 
# Du kan generera en säker, slumpmässig sträng med kommandot: openssl rand -base64 32
NEXTAUTH_SECRET="DIN_NEXTAUTH_SECRET_HÄR"
NEXTAUTH_URL="http://localhost:3000" # Ändra om du kör på en annan domän

# Google Generative AI (Gemini)
# Detta är din API-nyckel för att kunna göra anrop till Gemini-modellen.
# Skapa en nyckel i Google AI Studio.
# OBS! Utan denna nyckel kommer AI-chatten inte att fungera.
GEMINI_API_KEY="DIN_GEMINI_API_KEY_HÄR"

# Firebase Admin SDK (för server-side anrop)
# Dessa är dina service account-nycklar. Förvara dem säkert.
# OBS! Du måste lägga till radbrytningar (\n) i private key-strängen manuellt i din .env.local-fil.
FIREBASE_PROJECT_ID="DITT_FIREBASE_PROJEKT_ID"
FIREBASE_CLIENT_EMAIL="DITT_FIREBASE_CLIENT_EMAIL"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...DIN_PRIVATE_KEY...\n-----END PRIVATE KEY-----\n"
