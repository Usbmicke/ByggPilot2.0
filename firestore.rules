rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Neka all klient-åtkomst. All databasinteraktion måste ske
    // via säkra server-side API-routes som använder Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
