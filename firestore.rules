rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tillåt en användare att skapa sitt eget dokument
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
